[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ascii-archive"
version = "0.2.0"
dependencies = []

[project.scripts]
ascii-archive = "ascii_archive:main"
