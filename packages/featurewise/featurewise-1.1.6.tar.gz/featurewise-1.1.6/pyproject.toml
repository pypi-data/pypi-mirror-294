[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "featurewise"
version = "1.1.6"
description = "Your package description"
authors = ["Ambily Biju <ambilybiju2408@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
