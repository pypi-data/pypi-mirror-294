[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jlm_echo_plugin"
version = "0.1.1"
dependencies = ["llm", "tlid"]

[project.entry-points."llm.models"]
echo = "jecho:JEchoModel"
