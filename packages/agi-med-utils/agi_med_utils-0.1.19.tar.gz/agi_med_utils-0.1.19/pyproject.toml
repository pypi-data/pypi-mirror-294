[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = 'agi_med_utils'

authors = [{ name = 'AGI-MED-TEAM' }]
description = 'Utils for agi-med team'
readme = 'README.md'

dynamic = ['version', 'dependencies']

# dependencies = [
#     'pyyaml==6.0.1',
#     'gigachat==0.1.17',
#     'requests==2.31.0',
#     'python-json-logger==2.0.7',
#     'tqdm==4.66.2',
#     'dacite==1.8.1',
# ]

# [tool.setuptools]
# package-dir = { "" = "src" }


[tool.setuptools.packages.find]
where = ["src"]
# include = ['agi_med_utils*']
# exclude = ['test']
# namespaces = false

# packages = [""]

# [tool.setuptools]
# packages = ['config', 'dig_ass', 'llm', 'logger']

# [tool.setuptools.packages.find]
# where = ['src']
# include = ["*"]
# exclude = ["*test*"]

[tool.setuptools.dynamic]
version = { attr = "agi_med_utils.__version__" }
dependencies = { file = ['requirements.txt'] }
