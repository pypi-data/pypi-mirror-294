[tool.poetry]
name = "gyankosh"
version = "0.0.1"
description = "Unified, security-first, user-local knowledgebase with built-in RAG and Vector Store"
authors = ["Tridib Roy Arjo"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
