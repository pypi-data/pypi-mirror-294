from setuptools import setup, find_packages

setup(
    name = "namaste-samsara",
    version = "0.1",
    packages = find_packages(),
    install_requires = [
        # Nothing, this is just a learning project
    ],
)