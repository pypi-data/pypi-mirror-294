[tool.poetry]
name = "image-processing-navpack"
version = "0.1.3"  # Atualize para uma nova versão
description = "A package for processing and analyzing images."
authors = ["Fabiano <nav.info.suporte@gmail.com>"]
license = "MIT"
readme = "README.md"  # Adicione esta linha para incluir o README

[tool.poetry.dependencies]
python = ">=3.9"
numpy = "^1.26.4"
matplotlib = "^3.9.2"
scikit-image = "^0.24.0"
imageio = "^2.35.1"

[tool.poetry.dev-dependencies]
pytest = "^8.3.2"

[tool.poetry.urls]
repository = "https://github.com/Fabianonavarro/image-processing-navpack"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
