[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lupin_conan_report"
version = "1.0.0"
dependencies = [
  "jinja2==3.1.4",
  "typer==0.12.3",
]
requires-python = ">=3.10"
authors = [
  {name = "LupinDental", email = "contact@lupindental.com"}
]
maintainers = [
  {name = "LupinDental", email = "contact@lupindental.com"}
]
description = "Tool used to generate a report about they SOUPS."
readme = "README.md"
license = {file = "LICENSE"}
classifiers = [
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Development Status :: 3 - Alpha",
]

[project.optional-dependencies]
dev = [
    "coverage",  # testing
    "pytest",  # testing
]

[project.urls]
Repository = "https://gitlab.com/lupindental/arsene/devops/lupin_conan_report"

[project.scripts]
conan-report = "conan_report.run:cli"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
"*" = ["*.*"]